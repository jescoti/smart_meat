/**
 * Shared TypeScript types for the Smart Meat frontend.
 *
 * Feature-specific types live alongside their modules; this file holds
 * cross-cutting types shared between multiple features.
 */

// Stub — feature types will be added as work units are implemented.
